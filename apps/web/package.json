{
  "name": "@contexted/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build --prefix ../../packages/shared && vite",
    "build": "npm run build --prefix ../../packages/shared && vite build",
    "preview": "vite preview",
    "typecheck": "npm run build --prefix ../../packages/shared && tsc -p tsconfig.json --noEmit",
    "lint": "echo 'lint: web (no-op)'",
    "test": "npm run build --prefix ../../packages/shared && vitest run",
    "test:coverage": "npm run build --prefix ../../packages/shared && vitest run --coverage"
  },
  "dependencies": {
    "@contexted/shared": "0.1.0",
    "@tailwindcss/vite": "^4.2.1",
    "@tanstack/react-router": "^1.105.1",
    "motion": "^12.34.5",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "tailwindcss": "^4.2.1"
  },
  "devDependencies": {
    "@types/react": "^19.0.10",
    "@types/react-dom": "^19.0.4",
    "@vitejs/plugin-react": "^4.4.1",
    "@vitest/coverage-v8": "^3.0.8",
    "jsdom": "^26.0.0",
    "typescript": "^5.7.3",
    "vite": "^6.1.0",
    "vitest": "^3.0.8"
  }
}
