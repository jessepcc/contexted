{
  "name": "@contexted/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared schemas, state machines, sanitization, and matching logic for Contexted",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/jessepcc/contexted.git"
  },
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'lint: shared (no-op)'",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.8",
    "@vitest/coverage-v8": "^3.0.8"
  }
}
